<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.platform.admin.mapper.ProductDictMapper">

    <sql id="Base_Column_List">
        ID,DICT_TYPE, DICT_CODE, DICT_NAME, SORT_NO, I18N_CODE, DESCRIPTION, IS_ENABLED, START_DATE_ACTIVE,
         END_DATE_ACTIVE, CREATE_DATE, CREATE_BY, LAST_UPDATE_DATE, LAST_UPDATE_BY
    </sql>

    <select id="qryChildren" parameterType="com.platform.product.entity.admin.ProductDict"
            resultType="com.platform.product.entity.admin.ProductDict">
        SELECT  <include refid="Base_Column_List"/>
        FROM
            product_dict
        WHERE
            EXISTS ( SELECT DICT_TYPE FROM product_dict WHERE DICT_CODE = '%'
              <if test="dictName != null and dictName != ''">
                  AND DICT_NAME like concat('%',#{dictName},'%')
              </if>
               )
            AND DICT_CODE != '%'
            <if test="dictType != null and dictType != ''">
                AND DICT_TYPE = #{dictType}
            </if>
    </select>


</mapper>